首先理解题意

dp[i][i]表示的就是 i-1 j-1  之前的元素匹配


if p[i-1] != '*'   dp[i-1][j-1]需要匹配  并且   s[i-1]  p[i-1]  匹配
else if p[i-1] == '*'  有两种情况:
     0次匹配   p[j-1]之前的元素已经匹配   dp[i][j-2]  之前的元素也需要为0
     1次或者多次匹配 dp[i-1][j]  需要已经满足  并且新的元素需要和该元素重复的元素相同

.匹配的是所有单个字符
*匹配的是前面所有的字符  所以匹配时要检查是否和前面的元素是否相同